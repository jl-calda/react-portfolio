import { motion } from "framer-motion";

import LineGradient from "../components/LineGradient";

const Testimonials = () => {
  const testimonialStyle = `mx-auto relative max-w-[400px] h-[350px] flex flex-col justify-end pt-16 mt-48 before:absolute before:top-[-120px] before:-ml0[110px] before:left-1/2`;

  return (
    <section id="testimonial" className="pt-32 pb-16">
      {/* HEADING */}
    </section>
  );
};

export default Testimonials;
